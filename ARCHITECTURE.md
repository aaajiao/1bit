# 代码架构指南

> 在添加新功能前请先阅读此文件

---

## 📁 目录结构

```
src/
├── main.js          # 仅负责：初始化、组装系统、运行主循环
├── audio/           # 音效系统
├── player/          # 玩家相关（控制、手部、道具）
├── shaders/         # 着色器
├── world/           # 世界系统（区块、建筑、天气、昼夜...）
└── utils/           # 工具函数
```

---

## ✅ 新功能开发规则

### 1. 每个系统一个文件
- ❌ 不要在 `main.js` 中写业务逻辑
- ✅ 创建独立模块，如 `WeatherSystem.js`

### 2. 类的基本结构
```javascript
export class NewSystem {
    constructor() {
        // 初始化状态
    }

    update(delta, context) {
        // 每帧更新逻辑
        // context 包含需要的外部依赖
    }
}
```

### 3. main.js 只做三件事
```javascript
// 1. 导入系统
import { NewSystem } from './world/NewSystem.js';

// 2. 在 init() 中实例化
this.newSystem = new NewSystem();

// 3. 在 animate() 中更新
this.newSystem.update(delta, { /* 依赖 */ });
```

---

## 📂 放置位置

| 功能类型 | 目录 | 示例 |
|----------|------|------|
| 环境效果 | `world/` | WeatherSystem, DayNightCycle |
| 玩家相关 | `player/` | Controls, HandsModel |
| 渲染效果 | `shaders/` | DitherShader |
| 音效 | `audio/` | AudioSystem |
| 工具 | `utils/` | hash, ObjectPool |

---

## 🔄 重构信号

如果发现以下情况，应该重构：
- `main.js` 超过 300 行
- 同一功能的代码分散在多处
- 需要复制粘贴代码
